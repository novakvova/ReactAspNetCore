import React from 'react';

export default class Tags extends React.Component {

  render() {
    return (
      <ul>
        {/* { this.state.tags.map(tags =>  <li>{tags.name}{tags.id}</li>)} */}
      </ul>
    )
  }
}